name = "omarchy"
background = "{{ background }}"
surface = "{{ darker_background }}"
text = "{{ foreground }}"
accent = "{{ accent }}"
danger = "{{ red }}"
muted = "{{ muted }}"
highlight = "{{ selection }}"
border = "{{ light_foreground }}"
dim_text = "{{ dark_foreground }}"
